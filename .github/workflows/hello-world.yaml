name: Hello World

on:
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, World!"
  say-hello-again:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello Again
        run: echo "Hello again, World!"